# Google Gen AI Java SDK Function Call 示例总结

我已经为您创建了一个完整的Java function call示例项目，包含以下文件：

## 📁 项目文件

### 1. 主要示例文件
- **`FunctionCallExample.java`** - 完整的function call示例
- **`SimpleFunctionCallExample.java`** - 简化版示例，适合初学者

### 2. 配置文件
- **`function-call-example-pom.xml`** - Maven项目配置文件
- **`run_example.sh`** - 自动化运行脚本

### 3. 文档文件
- **`FunctionCallExample-README.md`** - 详细使用说明
- **`示例总结.md`** - 本文件

## 🚀 快速开始

### 方法1: 使用脚本运行（推荐）
```bash
# 设置API密钥
export GOOGLE_API_KEY=你的API密钥

# 运行脚本
./run_example.sh
```

### 方法2: 使用Maven
```bash
# 重命名pom文件
mv function-call-example-pom.xml pom.xml

# 设置API密钥
export GOOGLE_API_KEY=你的API密钥

# 编译并运行
mvn clean compile exec:java
```

### 方法3: 直接使用Java
```bash
# 下载SDK JAR文件
# 从 https://mvnrepository.com/artifact/com.google.genai/google-genai/1.23.0 下载

# 设置API密钥
export GOOGLE_API_KEY=你的API密钥

# 编译
javac -cp ".:google-genai-1.23.0.jar" FunctionCallExample.java

# 运行
java -cp ".:google-genai-1.23.0.jar" FunctionCallExample
```

## 🎯 示例功能

### 完整示例 (FunctionCallExample.java)
1. **天气查询函数** - 模拟查询指定城市的天气
2. **计算器函数** - 支持加减乘除运算
3. **用户信息查询** - 模拟数据库查询用户信息
4. **时间获取函数** - 获取当前时间
5. **聊天会话示例** - 展示在对话中使用函数
6. **错误处理示例** - 处理各种错误情况

### 简单示例 (SimpleFunctionCallExample.java)
1. **加法函数** - 简单的两数相加
2. **问候语函数** - 生成个性化问候语
3. **时间函数** - 获取当前时间

## 🔧 技术要点

### 1. 函数定义要求
- 必须是 `public static` 方法
- 参数类型要明确指定
- 编译时需要 `-parameters` 参数

### 2. 配置工具
```java
GenerateContentConfig config = GenerateContentConfig.builder()
    .tools(Tool.builder().functions(method1, method2))
    .build();
```

### 3. 调用AI
```java
GenerateContentResponse response = client.models.generateContent(
    "gemini-2.5-flash", 
    "你的提示词", 
    config
);
```

### 4. 获取函数调用历史
```java
response.automaticFunctionCallingHistory().orElse("无函数调用")
```

## 📋 使用步骤

1. **获取API密钥**
   - 访问 https://aistudio.google.com/app/apikey
   - 创建新的API密钥

2. **设置环境变量**
   ```bash
   export GOOGLE_API_KEY=你的API密钥
   ```

3. **选择运行方式**
   - 使用脚本：`./run_example.sh`
   - 使用Maven：`mvn exec:java`
   - 直接Java：编译后运行

4. **查看结果**
   - 观察函数调用过程
   - 查看AI的回复
   - 检查函数调用历史

## 🎨 自定义扩展

您可以基于这些示例添加自己的函数：

```java
// 添加新函数
public static String myFunction(String param) {
    // 你的逻辑
    return "结果";
}

// 在配置中添加
Method myMethod = MyClass.class.getMethod("myFunction", String.class);
config = GenerateContentConfig.builder()
    .tools(Tool.builder().functions(myMethod))
    .build();
```

## ⚠️ 注意事项

1. **API密钥安全** - 不要将API密钥提交到代码仓库
2. **网络连接** - 确保能访问Google API
3. **编译参数** - 必须使用 `-parameters` 参数
4. **函数限制** - 函数必须是静态的
5. **错误处理** - 添加适当的异常处理

## 🔗 相关链接

- [Google Gen AI Java SDK](https://github.com/googleapis/java-genai)
- [Gemini API 文档](https://ai.google.dev/gemini-api/docs)
- [Maven 中央仓库](https://mvnrepository.com/artifact/com.google.genai/google-genai)

## 📞 支持

如果遇到问题，请检查：
1. Java版本（需要Java 8+）
2. API密钥是否正确设置
3. 网络连接是否正常
4. 编译参数是否正确

祝您使用愉快！🎉